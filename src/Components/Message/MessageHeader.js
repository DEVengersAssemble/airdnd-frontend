import React from 'react';
import styled from 'styled-components';
// import theme from '../../style/theme';

const MessageHeader = () => {
  return <MessageHedarDiv>message header</MessageHedarDiv>;
};

const MessageHedarDiv = styled.div`
  background: orange;
  height: 7.5rem;
`;

export default MessageHeader;
