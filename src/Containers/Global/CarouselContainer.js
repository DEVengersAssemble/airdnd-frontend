import React from 'react';
import Carousel from '../../Components/Global/Carousel';

const CarouselContainer = ({ size, img }) => {
  return <Carousel size={size} img={img} />;
};

export default CarouselContainer;
