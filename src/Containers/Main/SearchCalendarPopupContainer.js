import React, { useState } from 'react';

const SearchCalendarPopupContainer = () => {
  const []
  // checkIn, checkOut, flexibleDate, currentMonth, monthArr
  
  
  // !checkIn =>
  // 현재 날짜를 가져온다.
  // [7, 8, 9, 1]
  // 

  return <div></div>;
};

export default SearchCalendarPopupContainer;
