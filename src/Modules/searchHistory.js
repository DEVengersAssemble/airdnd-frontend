const initialState = {
  place: '제주도',
  dateFrom: '',
  dateTo: '',
  traveller: 1,
};

const searchHistory = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default searchHistory;
